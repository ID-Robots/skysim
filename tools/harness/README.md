Conformance + scale harnesses. `conformance.py` is the acceptance gate (M2+).
M4 adds `churn.py` (spawn/despawn under load) and `scale.py` (N vehicles, tick p99 report).
Requires: ARDUPILOT_ROOT env var, `pip install pymavlink`.
