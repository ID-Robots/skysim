// tile_cooker: OBJ/glTF tile -> Jolt MeshShape -> SaveBinaryState -> <x>_<y>.jshape (+ index).
// Guarantees outward winding (mesh collision is one-sided). Python pre-step (decimate/tile)
// lives beside this in tools/cooker/pretile.py (M5). See docs/DESIGN.md "Terrain".
#include <cstdio>
int main() { std::printf("tile_cooker: implement at M5 per docs/MILESTONES.md\n"); return 0; }
